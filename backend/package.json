{
  "name": "backend",
  "version": "0.0.1",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "dev": "ts-node-dev --respawn --quiet src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^17.0.40",
    "rimraf": "^3.0.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.3"
  }
}